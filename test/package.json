{
  "name": "focustube-test",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node igfeed.test.mjs && node lifeed.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  },
  "//": "jsdom 27 silently drops !important from setProperty(); 29 keeps it, as browsers do, which is what the height-preservation assertions rely on."
}
